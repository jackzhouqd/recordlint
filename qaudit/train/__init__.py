"""印章状态模型：样本库、标注、合成、训练、版本注册。

设计原则（与主系统一致，不可让步）：

1. **训练可以依赖 sklearn，部署不引入任何新依赖**——线性模型导出为 npz，
   推理十几行 numpy 即可完成。
2. **模型只回答「这枚章是什么状态」**，判不判违规仍由 rules.yaml 决定，
   验收时能解释「为什么判它不合格」，不会变成黑盒。
3. **评估一律按源印章分组切分**——同一枚真实章派生的退化样本不得跨越
   训练/验证边界，否则准确率会被严重高估。
"""
from .features import LABEL_CN, LABEL_ORDER, featurize, load_linear, predict_one  # noqa: F401
