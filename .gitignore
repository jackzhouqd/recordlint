# RecordLint（质录检）
#
# 任何真实客户记录不得进入本仓（见 CLAUDE.md）。仓内样本一律 tools/synth_forms.py 合成。

# ---------------------------------------------------------------- 派生物
# out/ 全部是可重新生成的产物，且含敏感内容：
#   .ocr_cache/  档案全文，敏感度高于图片本身（图片要人看，全文可全文检索）
#   dataset/     从档案裁出的印章小图（约 140M）
#   *//report.html、findings.json  审核报告，重跑 2 分钟即可再生
out/

# ---------------------------------------------------------------- 数据库
# 含人工判定、用户口令散列、样本标注、模型注册表。属运行数据，不进版本库。
*.db
*.db-shm
*.db-wal

# ---------------------------------------------------------------- 部署配置
# 档案来源（挂载点）里写的是这台机器上的实际路径——含本机盘符与共享名，
# 因机器而异，由界面「系统 → 档案来源」现场维护，不进版本库。
config/archives.yaml

# ---------------------------------------------------------------- 模型权重
# 由「模型」页训练产出，随样本增长而变，不是源码
*.npz

# ---------------------------------------------------------------- Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/

# ---------------------------------------------------------------- 编辑器与系统
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
.DS_Store

# ---------------------------------------------------------------- 日志
*.log

# 注：qaudit/web/static/vendor/ 下的 htmx 与 Chart.js **要纳入版本管理**。
# 它们是随包分发的交付件，离线环境无法联网重新获取，不能当成可再生的依赖。

# ---------------------------------------------------------------- 内部计划
# 含脱敏词表与原项目背景，不随公开仓发布
docs/superpowers/
